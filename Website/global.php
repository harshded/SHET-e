<?php
$base_url = 'http://localhost/shete/Website';
error_reporting(0);
// session_start();
// Start a session if one is not already started
if (session_status() == PHP_SESSION_NONE) {
    session_start();
}